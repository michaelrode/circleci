version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: Code has arrived
          command: |
            ls -al
            echo "There is your code"
      - run:
          name: Running in a new contaier
          command: |
            node -v
